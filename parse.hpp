#pragma once

#include <xtd/parse.hpp>

namespace ooasm{
  namespace parse{
    namespace _{
      STRING_(AL);
      STRING_(AH);
      STRING_(BL);
      STRING_(BH);
      STRING_(CL);
      STRING_(CH);
      STRING_(DL);
      STRING_(DH);
    }
  }
}